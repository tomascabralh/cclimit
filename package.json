{
  "name": "@tomascabralh/cclimit",
  "version": "0.1.0",
  "description": "Credential-free Claude Code status line: plan-limit usage (5h/7d), context, git branch and session cost. No token, no Keychain, no network.",
  "type": "module",
  "bin": { "cclimit": "bin/cclimit.js" },
  "engines": { "node": ">=18" },
  "publishConfig": { "access": "public" },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "vitest run"
  },
  "keywords": ["claude-code", "statusline", "usage", "rate-limit", "cli"],
  "repository": { "type": "git", "url": "git+https://github.com/tomascabralh/cclimit.git" },
  "author": "Tomas Cabral Hunter",
  "license": "MIT",
  "devDependencies": { "vitest": "^3.0.0" }
}
